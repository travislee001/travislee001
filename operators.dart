void main() {
  //ARITHMETIC OPERATORS
  //+ - * / %
  int a = 3;
  int b = 8;

  int remainder = b % a;
  print("B DIVIDED BY A IS:");
  print(b / a);
  print("REMAINDER:");
  print(remainder);
  // int sum = a + b;
  // // print(a + b);
  // print(sum);

  // int diff = b - a;
  // print(diff);
  // int mult = a * b;
  // double div = b / a;
  // int divd = b ~/ a;
  // print(div);
  // print(divd);

  // //calculate the average score of a student
  // int math = 80;
  // int eng = 60;
  // int phy = 76;
  // int chem = 69;

  // double average = (math + eng + phy + chem) / 4;
  // print(average);

  // //area of a circle
  // double pi = 3.142;
  // int r = 6;
  // double area = r * r * pi;
  // print(area);

  //RELATIONAL OPERATORS
  //<, >, ==, >=, <=
  int c = 9;
  int d = 7;
  // print(c < d);
  // print(d > c);
  // print(d == c);
  // print(d < d);
  // print(d >= d); //d greater than || equal to d
  // print(c == (207 - 198));

  //LOGICAL OPERATORS
  // ||(or), &&(and)

  // print(true || false);
  // bool a = true;
  // bool b = false;
  // print(a || a); //true
  // print(a || b); //true
  // print(a || b || a || a); //true
  // print(b || b || b || b); //false
  // print(a && b || a && b); //false
  // print((a || b) && (a || b)); //true
  // print(((c < d) || (d > c)) && ((d == c) || (d >= d)) && (c == (207 - 198)));
  //
  BUILD A CONSOLE APPLICATION THAT RECEIVES THE NAME OF A USER, THE NAME OF 3 COURSES, THE SCORES,
  CALCULATE THE SUM OF THE SCORES AND THEN THE AVERAGE
}
